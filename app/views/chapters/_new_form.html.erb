<%= form_for [@comic, @chapter], :html => { :multipart => true } do |f| %>
  <div class='field'>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>

  <%= f.fields_for :chapter_attachments do |chapter_attachment| %>
    <div class='field'>
      <%= chapter_attachment.label :picture %><br>
      <%= chapter_attachment.file_field :picture, :multiple => true, name: 'chapter_attachments[picture][]' %>
    </div>
  <% end %>
  <div id='preview'></div>
  <div class='actions'>
    <%= f.submit %>
  </div>
<% end %>
