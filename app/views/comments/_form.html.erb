<%= form_for [@comic, @chapter, comment], remote: true  do |f| %>
  <div class='field'>
    <%= f.text_area :content, placeholder: t('.add') %>
  </div>
  <div class='actions'>
    <%= f.submit %>
  </div>
<% end %>
