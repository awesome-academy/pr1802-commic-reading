$('.list-group').prepend('<%= escape_javascript(render('comments/comment', comment: @comment)) %>');
$('#new_comment #comment_content').val('');
$('#comments').html('<%= t '.comments', count: @chapter.comments.count %>');
