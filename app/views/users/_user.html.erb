<li>
  <%= gravatar_for user, size: 40 %>
  <%= user.name %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t('.delete'), user, method: :delete,
      data: { confirm: t('.sure') } %>
  <% end %>
</li>
